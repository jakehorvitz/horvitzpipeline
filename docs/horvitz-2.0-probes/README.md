# Spec-time platform probes for Horvitz 2.0 owner-auth (2026-08-21)

Source + captured output of the two probes cited in spec v2 section 7. Rebuild with `swiftc -O <file>.swift`; probe 2 writes msg.txt/sig.der/pub.x963 for the openssl verification shown in its log.
