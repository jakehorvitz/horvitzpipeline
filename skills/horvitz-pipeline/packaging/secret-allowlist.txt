# Exact secret exceptions only. One tab-delimited finding per line:
# sha256-of-secret<TAB>RULE_ID<TAB>relative/path
#
# Do not add path globs or raw secrets. FORBIDDEN_PRIVATE_PATH findings cannot be
# allowlisted; remove those files from the gift candidate instead.
